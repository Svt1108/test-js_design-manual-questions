export enum Route {
  index = "index",
  login = "login",
  fio = "fio",
  theme = "theme",
  questions = "questions",
  result = "result",
  result_info = "result_info",
  default = "",
}

export enum LoginPass {
  login = "user-test",
  password = "user-test",
}
